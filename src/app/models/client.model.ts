export interface client {
  id?: number;
  nomClient: string,
  prenomClient: string,
  statutClient: string,
  photoClient: string,
  telClient: string,
  emailClient: string,
  adressClient: string,
  villeClient: string
}
